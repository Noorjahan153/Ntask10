name: Strapi ECS Deployment

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:

    #####################################
    # Checkout Code
    #####################################

    - name: Checkout Repository
      uses: actions/checkout@v3

    #####################################
    # Configure AWS Credentials
    #####################################

    - name: Configure AWS Credentials
      uses: aws-actions/configure-aws-credentials@v2
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-region: us-east-1

    #####################################
    # Setup Terraform
    #####################################

    - name: Setup Terraform
      uses: hashicorp/setup-terraform@v2

    - name: Terraform Init
      working-directory: terraform
      run: terraform init

    - name: Terraform Plan
      working-directory: terraform
      run: terraform plan

    #####################################
    # Docker Build + Push to ECR (Manual Repo)
    #####################################

    - name: Login to Amazon ECR
      uses: aws-actions/amazon-ecr-login@v1

    - name: Build Docker Image
      run: |
        docker build -t strapi-app ./strapi-app

    - name: Tag Docker Image
      run: |
        docker tag strapi-app:latest 811738710312.dkr.ecr.us-east-1.amazonaws.com/noor-strapi:latest

    - name: Push Docker Image
      run: |
        docker push 811738710312.dkr.ecr.us-east-1.amazonaws.com/noor-strapi:latest

    #####################################
    # Terraform Apply
    #####################################

    - name: Terraform Apply
      working-directory: terraform
      run: terraform apply -auto-approve
